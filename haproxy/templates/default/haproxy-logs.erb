$ModLoad imudp
$UDPServerAddress 127.0.0.1
$UDPServerRun 514

if ( \
  $programname contains 'haproxy' and \
  ($syslogseverity <= 6 /* notice */ ) \
) then -<%= node[:haproxy][:log_dir] %>/haproxy.log

# Uncomment the following to stop logging anything that matches the last rule.
& ~

