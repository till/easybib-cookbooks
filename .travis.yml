language: ruby

sudo: false

cache: bundler

script:
  - bundle exec rake spec[easybib]

gemfile:
  - gemfiles/chef_11.10.gemfile
  - gemfiles/chef_12.13.gemfile

branches:
  only:
  - master
  - stable-chef-11.10
  - stable

notifications:
  email:
    on_success: never
    on_failure: always
