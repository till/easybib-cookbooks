language: ruby

gemfile:
  - gemfiles/chef11-gemfile

sudo: false

cache: bundler

script:
  - make spec

branches:
  only:
  - master
  - stable-chef-11.10
  - stable

notifications:
  webhooks: http://cd.deploy.ezbib.com/travis/update-cookbooks
  email:
    on_success: never
    on_failure: always
