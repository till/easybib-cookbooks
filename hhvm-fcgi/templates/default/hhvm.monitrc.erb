check process hhvm-server with pidfile "<%= @pid_file %>"
  start program = "/etc/init.d/<%= @service_name %> start"
  stop program = "/etc/init.d/<%= @service_name %> stop"
  group <%= @service_name %>
