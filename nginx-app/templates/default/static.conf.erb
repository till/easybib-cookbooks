server {
    listen 80;

    index <%=@default_router%> index.php;

<% if !@domain_name.empty? -%>
    server_name <%=@domain_name%>;
<% end -%>

    location / {
        try_files $uri $uri/;
    }

    root <%= @doc_root%>;
}
