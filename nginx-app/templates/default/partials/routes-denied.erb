    <% if !@routes_denied.nil? && !@routes_denied.empty? -%>
    location ~ /(<%= @routes_denied.join('|') %>) {
        deny all;
    }
    <% end -%>

    location /cgi-bin/ {
        return 404;
    }

    location ~ \.cgi$ {
        return 404;
    }
