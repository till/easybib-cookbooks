<%
if @enabled
  @config.each do |location, conf|
-%>
    if ( $uri ~ .(<%= location %>)$  ) {
        expires <%= conf['expires'] %>;
<%
    unless conf['headers'].nil?
      conf['headers'].each do |header|
-%>
        <%= "add_header #{header}" %>;
<%
      end
    end
%>
    }
<%
  end
end
-%>
