server {
    listen      80;
    server_name <%=@ip%>;

    access_log  /dev/null;

    location / {
        proxy_pass        http://127.0.0.1:8091;
        proxy_set_header  X-Real-IP  $remote_addr;
    }
}
