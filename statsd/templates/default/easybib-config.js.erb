{
  port: <%= node[:librato][:node][:port] %>,
  mgmt_address: '127.0.0.1',
  backends: ['statsd-librato-backend'],
  librato: {
    email: '<%= node[:librato][:metrics][:email] -%>',
    token: '<%= node[:librato][:metrics][:api_key] -%>',
    source: '<%= node[:opsworks][:stack][:name] -%>',
    batchSize: <%= node[:librato][:metrics][:batch] %>
  }
}
